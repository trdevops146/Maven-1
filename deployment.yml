kind: Deployment
apiVersion: apps/v1
metadata:
  name: cicd-deployment
spec:
  replicas: 3
  selector:
    matchLabels:
      app: kubeserve
  template:
    metadata:
      name: kubeserve
      labels:
        app: kubeserve
    spec:
      containers:
       - name: app
         image: sonal04/myimageprojectdevops
        
---
kind: Service
apiVersion: v1
metadata:
   name: kubeserve-svc
spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
  selector: 
    app: kubeserve
